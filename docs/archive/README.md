# Archive — do not build from these

Superseded planning documents, kept only so decisions can be traced back.
**They describe types, file layouts and build phases that no longer exist.**

| File | Superseded by | Why |
|---|---|---|
| `TECHNICAL_PLAN_v2.md` | [ARCHITECTURE.md](../ARCHITECTURE.md), [TECHNICAL_PLAN.md](../TECHNICAL_PLAN.md), [PORT_INVENTORY.md](../PORT_INVENTORY.md), [ROADMAP.md](../ROADMAP.md) | A merge of a v1 and a v2 plan that carried both as inline history. Its Part A described `ChartID`, `chartKey`/`chartShape`, a single `QuizService`, a flat `Question`, a `WordSpec` with an optional slot, and the `manualTables` fixture fallback — all removed in the Aug 2026 restructure. Its Part C phase table predates the v1-scope decision, and the v1 tables inside it appear twice. |

If you are an agent: you have no reason to open anything in this directory.
